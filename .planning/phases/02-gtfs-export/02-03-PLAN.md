---
phase: 02-gtfs-export
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - backend/app/routers/gtfs.py
  - backend/tests/test_gtfs_export.py
  - .planning/phases/02-gtfs-export/02-VALIDATION.md
autonomous: false
requirements: [EXPORT-09, EXPORT-10]

must_haves:
  truths:
    - "A real exported zip uploaded to gtfs-validator.mobilitydata.org reports zero ERROR-level violations"
    - "Any spec violation found during validation is fixed in the builders and locked by a regression test"
    - "The browser download path works end-to-end (click → file downloads with correct name and zip MIME)"
  artifacts:
    - path: ".planning/phases/02-gtfs-export/02-VALIDATION.md"
      provides: "documented manual gtfs-validator procedure + recorded result"
      contains: "gtfs-validator"
    - path: "backend/tests/test_gtfs_export.py"
      provides: "regression tests for any validator-discovered fixes"
      contains: "def test_"
  key_links:
    - from: "exported zip"
      to: "MobilityData gtfs-validator"
      via: "manual upload at gtfs-validator.mobilitydata.org"
      pattern: "gtfs-validator"
---

<objective>
Prove the exported zip is spec-compliant: generate a real GTFS zip from a saved reroute, run it through MobilityData's gtfs-validator, fix any ERROR-level violations in the builders, and lock each fix with a regression test. Then confirm the full browser download works end-to-end via a human-verify checkpoint.

Purpose: Closes EXPORT-09 (zero validator ERRORs) — the phase's headline success criterion — and confirms EXPORT-10/EXPORT-01 in a real browser. This is the slice that turns "produces a zip" into "produces a VALID zip a user actually receives."
Output: Any builder fixes in gtfs.py + regression tests; recorded validation result in 02-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gtfs-export/02-RESEARCH.md
@.planning/phases/02-gtfs-export/02-VALIDATION.md

<interfaces>
backend/app/routers/gtfs.py (from 02-01): export_gtfs endpoint + all _build_* DataFrame builders + _write_feed.
Known validator ERROR rules to confirm (RESEARCH §Common Pitfalls):
  - stop_time_with_only_arrival_or_departure_time → all stop_times rows need BOTH times (Pitfall 1)
  - invalid_input_files_in_subfolder → all 8 files at zip root (Pitfall 5)
  - route_color invalid format → bare 6-hex, no # (Pitfall 4)
  - swapped shape coords → lat/lon order (Pitfall 2)
Manual validator: gtfs-validator.mobilitydata.org (web UI; no Java/pip needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate a real GTFS zip and run gtfs-validator</name>
  <files>.planning/phases/02-gtfs-export/02-VALIDATION.md, backend/app/routers/gtfs.py, backend/tests/test_gtfs_export.py</files>
  <read_first>
    - backend/app/routers/gtfs.py (builders + endpoint to fix if violations found)
    - .planning/phases/02-gtfs-export/02-RESEARCH.md §Common Pitfalls + §GTFS Spec: Required Columns per File
    - .planning/phases/02-gtfs-export/02-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
    Produce a real zip without depending on a live Supabase reroute: write a short throwaway script (e.g. `backend/scripts/_gen_sample_gtfs.py`, gitignored or deleted after) that imports the builders from app.routers.gtfs, constructs a representative multi-route reroute in-memory (reuse the conftest fixture shape: two routes, a shared stop, one synthetic stop, >=5 stops each so shapes/stop_times are non-trivial), builds the gtfs_kit.Feed, calls `_write_feed`, and writes the bytes to `/tmp/mtd-export-sample.zip`. Run it: `cd backend && python scripts/_gen_sample_gtfs.py`.

    Upload `/tmp/mtd-export-sample.zip` to https://gtfs-validator.mobilitydata.org (web UI). Record the result. If the validator reports any ERROR-level violations (WARNINGs are acceptable per EXPORT-09):
    - Diagnose the offending file/column against RESEARCH §GTFS Spec required columns and §Common Pitfalls.
    - Fix the corresponding `_build_*` function in gtfs.py with the concrete correction (e.g. add a missing required column, correct a value format).
    - Add a regression test to test_gtfs_export.py asserting the specific corrected behavior (named after the rule, e.g. `test_no_missing_stop_time`), and re-run `cd backend && python -m pytest tests/ -q` until green.
    - Regenerate the zip and re-upload until zero ERRORs.

    Record in 02-VALIDATION.md (new `## Validator Result` section): date run, validator version/agency reported, the count of ERRORs (must reach 0) and WARNINGs, and a list of any fixes applied with the test that now guards each. Update the frontmatter `nyquist_compliant` / sign-off checkboxes as appropriate. Delete the throwaway script when done (it must not be committed as production code).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - 02-VALIDATION.md contains a `## Validator Result` section recording 0 ERROR-level violations and the run date
    - Any validator-driven fix in gtfs.py has a corresponding regression test in test_gtfs_export.py
    - `cd backend && python -m pytest tests/ -q` exits 0
    - The throwaway generation script is not present under version control (grep git-tracked files for `_gen_sample_gtfs` returns nothing, or it lives under a gitignored path)
  </acceptance_criteria>
  <done>A real generated zip passes gtfs-validator with zero ERRORs; every fix is locked by a regression test; result recorded in 02-VALIDATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — end-to-end browser GTFS download</name>
  <files>(verification only — no files modified)</files>
  <action>
    This is a human-verify checkpoint, not an implementation task. All code is complete (02-01 endpoint, 02-02 UI, Task 1 validator compliance). The executor pauses here and presents the verification steps below to the user, then waits for the resume signal before marking the phase verifiable. Do not modify code in this task; if the user reports a defect, capture it for a gap-closure plan.
  </action>
  <what-built>
    Backend export endpoint (02-01), frontend FileArchive button + download handler (02-02), and validator-confirmed spec compliance (Task 1 above). This checkpoint confirms the complete user flow in a real browser against a running stack.
  </what-built>
  <how-to-verify>
    1. Start the stack (backend: `cd backend && uvicorn app.main:app --reload`; frontend: `cd frontend && npm run dev`).
    2. Sign in, open the Reroutes dashboard.
    3. Pick a reroute that contains at least one saved route. Confirm a FileArchive (zip) icon button appears to the LEFT of the PNG Download icon in that reroute's row.
    4. Click the FileArchive button. Expected: the icon briefly becomes a spinning Loader2, then the browser downloads a file named `{reroute name}-gtfs.zip`.
    5. Open the downloaded zip. Expected: 8 files at the root (agency.txt, routes.txt, trips.txt, stops.txt, stop_times.txt, shapes.txt, calendar_dates.txt, feed_info.txt) and no subfolders.
    6. Confirm a reroute with NO saved routes shows the FileArchive button disabled (greyed/opacity-40, tooltip "No routes to export").
    7. (Optional) Re-upload this real exported zip to gtfs-validator.mobilitydata.org to reconfirm zero ERRORs on actual user data.
  </how-to-verify>
  <verify><human-check>User confirms download works, zip has 8 root-level files, and disabled state behaves.</human-check></verify>
  <resume-signal>Type "approved" if the download works, the zip has 8 root-level files, and the disabled state behaves; otherwise describe the issue.</resume-signal>
  <done>User has approved the end-to-end browser download flow, or reported defects captured for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exported zip → external validator | User-controlled route data serialized into a GTFS feed; validated for spec conformance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | malformed route data during zip build | mitigate | Validation run exercises real multi-route data including synthetic stops; any builder crash on edge data is fixed + regression-tested here. |
| T-02-03 | Availability | OSRM during real export | accept | D-06 fallback already covered by 02-01 test; manual run may exercise live OSRM but failure degrades to straight-line, never errors. |
| T-02-SC | Tampering | dependencies | mitigate | No new packages; throwaway generation script is deleted, not committed. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/ -q` exits 0 after any fixes.
- 02-VALIDATION.md `## Validator Result` records 0 ERRORs.
- Human-verify checkpoint approved for the browser download flow.
</verification>

<success_criteria>
- Exported zip passes MobilityData gtfs-validator with zero ERROR-level violations (EXPORT-09).
- feed_info.txt present and accepted by the validator (EXPORT-10 confirmed on real data).
- End-to-end browser download confirmed by human-verify.
</success_criteria>

<output>
Create `.planning/phases/02-gtfs-export/02-03-SUMMARY.md` when done.
</output>
