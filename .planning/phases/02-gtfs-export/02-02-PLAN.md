---
phase: 02-gtfs-export
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/lib/api.ts
  - frontend/components/RerouteDashboard.tsx
autonomous: true
requirements: [EXPORT-01]

must_haves:
  truths:
    - "RerouteDashboard shows a FileArchive icon button in each reroute row, left of the PNG Download button"
    - "Clicking the GTFS button for a reroute with routes downloads a .zip named {reroute.name}-gtfs.zip"
    - "The GTFS button shows a Loader2 spinner while exporting and is disabled during export"
    - "The GTFS button is disabled (opacity-40) when the reroute has no saved routes"
    - "GTFS export state is independent of PNG export state (separate exportingGtfsRerouteId)"
  artifacts:
    - path: "frontend/lib/api.ts"
      provides: "exportGtfs(rerouteId, token) fetch helper returning Blob"
      contains: "export async function exportGtfs"
    - path: "frontend/components/RerouteDashboard.tsx"
      provides: "FileArchive button + handleExportGtfs + exportingGtfsRerouteId state"
      contains: "handleExportGtfs"
  key_links:
    - from: "frontend/components/RerouteDashboard.tsx handleExportGtfs"
      to: "frontend/lib/api.ts exportGtfs"
      via: "imported call passing reroute.id + bearer token"
      pattern: "exportGtfs\\("
    - from: "frontend/lib/api.ts exportGtfs"
      to: "/api/gtfs/export/{rerouteId}"
      via: "fetch with Authorization Bearer header (proxied by next.config rewrite)"
      pattern: "api/gtfs/export"
---

<objective>
Complete the user-facing vertical slice: add the `exportGtfs` API helper and a `FileArchive` icon button to `RerouteDashboard` so a user can click and download the GTFS zip produced by the 02-01 backend endpoint. After this plan, a user can do something they could not before — trigger and receive a GTFS download from the editor.

Purpose: Closes EXPORT-01 (user-triggered download). The BFF rewrite already proxies `/api/gtfs/export/*`, so no new Next.js route handler is needed.
Output: `exportGtfs` in api.ts; FileArchive button, handler, and state in RerouteDashboard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gtfs-export/02-CONTEXT.md
@.planning/phases/02-gtfs-export/02-UI-SPEC.md
@.planning/phases/02-gtfs-export/02-PATTERNS.md

<interfaces>
<!-- Existing frontend code to build against. -->

frontend/lib/api.ts:
  const BASE = ""  // fetches are relative/same-origin; Next.js rewrite proxies /api/* to backend
  export async function exportPng(payload): Promise<Blob>  // analog: fetch → if (!res.ok) throw → res.blob()
  export interface Reroute { ... saved_routes: SavedRouteRef[] }  // SavedRouteRef = {id,name,short_name,is_custom}; NO route_stops on frontend (backend fetches those)

frontend/components/RerouteDashboard.tsx:
  line 14: import { X, ChevronDown, ChevronRight, Trash2, Plus, Download, Loader2, LogIn, Pencil } from "lucide-react"
  line 29: const [exportingRerouteId, setExportingRerouteId] = useState<string|null>(null)
  ~line 203: async function handleExportAll(reroute: Reroute)  // PNG blob-download analog (URL.createObjectURL + anchor click + revokeObjectURL)
  ~line 425-433: existing Download (PNG) icon button JSX in the reroute row action strip
  `token` is available in component scope (used by handleExportAll for auth)

backend endpoint (from 02-01): GET /api/gtfs/export/{reroute_id}, Authorization: Bearer <token>, returns application/zip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exportGtfs helper to api.ts</name>
  <files>frontend/lib/api.ts</files>
  <read_first>
    - frontend/lib/api.ts (BASE constant, exportPng analog, Reroute/SavedRouteRef types)
    - .planning/phases/02-gtfs-export/02-PATTERNS.md (api.ts §exportGtfs pattern)
  </read_first>
  <action>
    Add an exported async function `exportGtfs(rerouteId: string, token: string): Promise<Blob>` near `exportPng`. It performs `fetch(`${BASE}/api/gtfs/export/${rerouteId}`, { headers: { Authorization: `Bearer ${token}` } })`. Method is GET (no body, no Content-Type). If `!res.ok`, `throw new Error(`GTFS export failed: ${res.statusText}`)`. Return `res.blob()`. Add a JSDoc block comment matching the file's documentation style (other lib functions use `/** ... */`).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -c "api.ts" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - api.ts contains `export async function exportGtfs(rerouteId: string, token: string): Promise<Blob>`
    - The function fetches `/api/gtfs/export/${rerouteId}` with an `Authorization: Bearer` header and no request body
    - `cd frontend && npx tsc --noEmit` reports no errors in api.ts
  </acceptance_criteria>
  <done>exportGtfs exists, typed, GET with bearer auth, returns Blob; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: FileArchive export button + handler + state in RerouteDashboard</name>
  <files>frontend/components/RerouteDashboard.tsx</files>
  <read_first>
    - frontend/components/RerouteDashboard.tsx (lucide import line 14, exportingRerouteId state line 29, handleExportAll ~203, Download button JSX ~425-433)
    - frontend/lib/api.ts (exportGtfs added in Task 1 — import it)
    - .planning/phases/02-gtfs-export/02-UI-SPEC.md (Interaction Contract, States, Copywriting)
    - .planning/phases/02-gtfs-export/02-PATTERNS.md (RerouteDashboard §state/handler/button patterns)
  </read_first>
  <action>
    Add `FileArchive` to the lucide-react import on line 14. Add `exportGtfs` to the existing `@/lib/api` named-import statement.

    Add state directly below line 29: `const [exportingGtfsRerouteId, setExportingGtfsRerouteId] = useState<string | null>(null);`.

    Add `async function handleExportGtfs(reroute: Reroute)` modeled on handleExportAll but single-file (no loop, no inter-file delay): guard `if (!token || !reroute.saved_routes?.length) return;`; `setExportingGtfsRerouteId(reroute.id)`; in try, `const blob = await exportGtfs(reroute.id, token);` then the blob-download pattern (URL.createObjectURL → create anchor → set `a.download = `${reroute.name.replace(/[^\w\s\-().]/g, "_")}-gtfs.zip`` → a.click() → URL.revokeObjectURL(url)); in catch `console.error("GTFS export failed:", err)`; in finally `setExportingGtfsRerouteId(null)`.

    Insert a new icon `<button>` immediately BEFORE the existing PNG Download button in the reroute row action strip. Copy the Download button's className exactly (`text-muted-foreground hover:text-foreground shrink-0 disabled:opacity-40`). onClick `() => void handleExportGtfs(reroute)`. `disabled={exportingGtfsRerouteId === reroute.id || !reroute.saved_routes?.length}`. `title={!reroute.saved_routes?.length ? "No routes to export" : "Export GTFS zip"}`. aria-label "Exporting GTFS…" while in progress. Render `exportingGtfsRerouteId === reroute.id ? <Loader2 className="w-4 h-4 animate-spin" /> : <FileArchive className="w-4 h-4" />`.

    Per UI-SPEC: success is silent (browser download), no toast; error is silent with console.error. GTFS and PNG export states stay independent.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -c "RerouteDashboard" | grep -q "^0$" && npx eslint components/RerouteDashboard.tsx --max-warnings=0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - RerouteDashboard.tsx imports FileArchive from lucide-react and exportGtfs from @/lib/api
    - Contains `const [exportingGtfsRerouteId, setExportingGtfsRerouteId]` and `async function handleExportGtfs`
    - A FileArchive button renders before the Download button, disabled when `!reroute.saved_routes?.length`, with title "Export GTFS zip" / "No routes to export"
    - Download filename pattern is `{reroute.name sanitized}-gtfs.zip`
    - `cd frontend && npx tsc --noEmit` clean for RerouteDashboard.tsx; `npx eslint components/RerouteDashboard.tsx --max-warnings=0` exits 0
  </acceptance_criteria>
  <done>FileArchive button present with idle/disabled/in-progress states; handleExportGtfs downloads the zip via blob+anchor; independent of PNG state; type-check + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js BFF | Bearer token forwarded in fetch; proxied by existing /api/* rewrite |
| Next.js BFF → FastAPI | Existing rewrite; no new route handler introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing/EoP (IDOR) | exportGtfs request | mitigate | Auth enforced server-side (02-01 ownership check); frontend only forwards the user's own bearer token. No client-side trust. |
| T-02-04 | Information disclosure | error surface | accept | Errors are console.error only (no PII in zip-export failure messages); matches existing PNG export pattern per UI-SPEC. |
| T-02-SC | Tampering | npm installs | mitigate | No new npm packages; FileArchive already present in lucide-react 1.17.0 (verified RESEARCH §Standard Stack). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean.
- `cd frontend && npx eslint components/RerouteDashboard.tsx lib/api.ts --max-warnings=0` exits 0.
- Manual (deferred to 02-03): clicking the button in a running stack downloads a zip.
</verification>

<success_criteria>
- A FileArchive button in each reroute row triggers a GTFS zip download via the 02-01 endpoint.
- Download filename is `{reroute.name}-gtfs.zip` (sanitized).
- Button disabled with no routes; spinner during export; independent of PNG export.
- EXPORT-01 user-trigger path is implemented end-to-end (UI → BFF → FastAPI).
</success_criteria>

<output>
Create `.planning/phases/02-gtfs-export/02-02-SUMMARY.md` when done.
</output>
